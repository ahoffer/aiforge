# ---------- Proteus Smart Proxy ----------
apiVersion: v1
kind: ConfigMap
metadata:
  name: proteus-config
  namespace: aiforge
data:
  OLLAMA_URL: "http://ollama:11434"
  SEARXNG_URL: "http://searxng:8080"
  QDRANT_URL: "http://qdrant:6333"
  AGENT_MODEL: "devstral:latest-agent"
  AGENT_MAX_TOKENS: "2048"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: proteus
  namespace: aiforge
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: proteus
  template:
    metadata:
      labels:
        app: proteus
    spec:
      containers:
        - name: proteus
          image: proteus:bee4c491897a
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: proteus-config
          resources:
            requests:
              memory: 256Mi
              cpu: 250m
            limits:
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: proteus
  namespace: aiforge
spec:
  selector:
    app: proteus
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 31400
  type: NodePort
